  <div class="chat-footer">
      <%= simple_form_for([:employees,@chat, Message.new]) do |f| %>
        <%= f.input :content, label: false, placeholder: "質問してみましょう..." %>
        <%= f.submit "送信", class: "btn btn-primary", data: { turbo_frame: 'conversation-log' } %>
      <% end %>
  </div>
</div>
